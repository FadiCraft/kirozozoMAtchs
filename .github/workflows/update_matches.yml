name: Update Matches Data

on:
  schedule:
    - cron: '*/10 * * * *' # يعمل كل 10 دقائق
  workflow_dispatch: # يتيح لك تشغيله يدوياً للتجربة

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # خطوة جديدة: تثبيت مكتبات النظام الضرورية لتشغيل متصفح Puppeteer على سيرفرات أوبونتو
      - name: Install Puppeteer System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgbm-dev libnss3 libatk-bridge2.0-0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libasound2

      - name: Install Node Dependencies
        run: npm install axios cheerio puppeteer

      - name: Run Scraper
        run: node index.js

      - name: Commit and Push Changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add match1.json
          git commit -m "Update matches data [skip ci]" || exit 0
          git push
